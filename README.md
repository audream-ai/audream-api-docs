# Audream API documentation

Public API documentation for Audream, built with Mintlify.

## Local preview

```bash
npm i -g mint
mint dev
```

Validate changes before publishing:

```bash
mint validate
mint broken-links
```

The production API base URL is `https://audream-api.tulingbc.com`.
